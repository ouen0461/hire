server:
  port: 8082
  servlet:
    context-path: /

  tomcat:
      relaxed-query-chars: '|,{,},[,]'

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hire?useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 123456
    hikari:
      maximum-pool-size: 30
      minimum-idle: 30
      connection-timeout: 5000
      idle-timeout: 0
      max-lifetime: 18000000

    #配置redis的连接信息
  data:
    redis:
      host: 8.147.235.190
      port: 6370
      password: 123456
      database: 1


#????mapper.xml?????
mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    #??????sql??
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

---
spring:
  profiles:
    active: dev  # 设置默认激活的环境